<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:label="TFG"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".DetalleLigaActivity" />
        <activity android:name=".DetallePremierActivity" />

        <activity android:name=".PrediccionActivity" />
        <activity android:name=".PremierPrediccionActivity" />

        <activity android:name=".EstadisticasEquipoActivity" />
        <activity android:name=".EstadisticasJugadorActivity" />
        <activity android:name=".EstadisticasPartidoActivity" />
        <activity android:name=".ComparadorActivity" />

    </application>

</manifest>